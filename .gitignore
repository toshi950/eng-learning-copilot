# Environment variables (contains secrets — never commit)
.env
.env.local
.env.*.local

# Credentials (GCP service account keys etc. — never commit)
credentials/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Generated audio (TTS output — not committed, regenerable)
*.mp3

# OS
.DS_Store
