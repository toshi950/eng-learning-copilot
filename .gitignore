# Environment variables (contains secrets — never commit)
.env
.env.local
.env.*.local

# Credentials (GCP service account keys etc. — never commit)
credentials/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Generated audio (TTS output — not committed, regenerable)
*.mp3

# Pipeline cache (last fetched article, for --from-cache)
.cache/

# OS
.DS_Store

# Local convenience launcher (hardcodes this machine's absolute path — not
# portable, personal tooling only)
eng-sync.command
